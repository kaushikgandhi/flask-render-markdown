# {{ title }}

All endpoints return JSON and require an `Authorization: Bearer <token>` header.

{% for ep in endpoints %}
## `{{ ep.method }} {{ ep.path }}`

{{ ep.description }}
{% endfor %}

---

Generated by flask-render-markdown for {{ request.headers.get("User-Agent", "an unknown client") }}.
